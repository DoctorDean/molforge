# data/

This directory is for small reference data assets that ship with the
repository. Large datasets, model weights, and PDB dumps must NOT be
committed here — keep them external and fetch on demand.

Test fixtures live under `tests/fixtures/`, not here.
